only public keys are stored in authorized_keys, private key/s is/are in 
home/USER/.ssh/id_rsa

simply, read the file at location ~/.ssh/authorized_keys

cat ~/.ssh/authorized_keys

# a deduction, written in your own words