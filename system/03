to permanently change the hostname, the file in /etc/hostname must be edited
with "sudo <editor> /etc/hostname" and the name of the localhost must be changed
to the desired new name

# a deduction, written in your own words